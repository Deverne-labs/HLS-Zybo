#include "color_to_gray_utils.h"

void ColorToGrayUnit(color_video_stream &stream_in,
					gray_video_stream &stream_out,
					int image_w,
					int image_h);

void ColorToGray(color_video_stream &stream_in,
				gray_video_stream &stream_out,
				int image_w,
				int image_h);
